import React from 'react';
import Auth from '../../components/auth/Auth';

const LoginPage = () => {
    return (
        <>
            <Auth isSignup={false} />
        </>
    )
};

export default LoginPage;
